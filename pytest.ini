[pytest]
testpaths = tests
addopts = -q
# The optimizer/dataset modules live in contest_solution/ and external/FloorSet/;
# only the curated suite under tests/ is collected. Files named *_test.py elsewhere
# are dataset helpers, not pytest tests.
