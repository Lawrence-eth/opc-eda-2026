[pytest]
testpaths = tests contest_solution/test_my_optimizer.py
addopts = -q
# Collect the curated suite plus the optimizer's own regression tests.  Other
# files named *_test.py under contest_solution/ are dataset helpers, so list
# the optimizer test file explicitly instead of scanning that whole directory.
