# ICCAD 2026 FloorSet Winning Execution Plan

> **Provenance:** Generated by Sisyphus (lead) via adversarial multi-agent planning (5 members, 3 rounds of cross-attack, verified by independent calculation). Plan agent: `ses_1ad0f2b8bffe6Wp4sZwTPHjBE0`.

## The True Score

**Corrected baseline score: ~2.91** (not 1.50 as previously reported).

The previous agent's analysis scripts used the wrong scoring formula `exp(n - max_n)` instead of the official `exp((n - max_n) / 12)`. This inflated score concentration and hid how bad the optimizer really is.

| Metric | Old Formula (WRONG) | Official Formula (TRUE) |
|---|---|---|
| Total Score | 1.51 | **2.91** |
| Case 99 weight | 63.21% | **8.00%** |
| Top 5 weight | 99.33% | **34.08%** |
| Top 20 weight | 100.00% | **81.13%** |

## Adversarial Synthesis

### Phase 1: Heuristic Fix (Unanimous — All 5 Members)

**Root cause identified by deep-diver:** The heuristic has exactly 3 fatal bugs:

1. **Aspect Ratio Mismatch:** Uses `row_factor = 1.10` for 120 blocks → wide layouts (AR > 1.0). Ground truth is tall/narrow (AR ~0.5).
2. **Boundary Packing Waste:** Boundary blocks placed STRICTLY OUTSIDE interior bbox. Preplaced blocks span large width → massive bbox (176x580 vs ground truth 131x265).
3. **Dead Compactor:** `_refine_boundary_edge_inward_compactions` aborts if ANY edge block is preplaced. High-weight cases have preplaced blocks on almost all edges → compactor never runs.

**Expected impact:** Score drop from ~2.91 to ~1.5. Maintains 100% feasibility.

### Phase 2: ML Global Placer (4/5 Consensus)

**Architecture:** Graph Transformer (not standard GNN, not diffusion)
- Predicts optimal Sequence Pair (SP) topological representation
- More robust than raw (x,y,w,h) — maps directly to deterministic overlap-free packing

**Training:** Pure supervised learning (concession by architect to strategist)
- Train on 1M ground truth samples using CrossEntropy/MSE on topological relations
- Do NOT use official differentiable loss (deep-diver proved it's a trap — ignores all placement constraints)
- Do NOT build custom physics loss (conceded by architect — over-engineered nightmare)

**Runtime target (mathematical proof by architect):**
- Cost = (1 + 0.5*Gaps) * exp(2*V) * max(0.7, R^0.3)
- To score < 1.0, MUST exploit runtime discount
- Target: < 100ms per case (GPU forward pass ~50ms + CPU legalizer ~10ms)

### Rejected Approaches

1. **Custom differentiable physics loss:** Over-engineered, likely gets stuck in local minima.
2. **Dictionary exploit / isomorphic lookup:** 3/5 members rejected as "suicide" — risk of novel topologies in hidden test set + potential disqualification.
3. **Specialist models per block count:** Rejected due to flatter weighting (top 20 = 81%, not just top 5).
4. **Pure heuristic without ML:** Pragmatist conceded this cannot get below 1.0.

## Execution Waves

### Wave 1 (Start Immediately)
- **Task 1:** Fix score tooling to use official `exp((n - max_n) / 12)` weighting
- **Task 5:** Build TDD tests for deriving Sequence Pair labels from ground truth

### Wave 2
- **Task 2:** Add failing regression tests for the 3 heuristic bugs
- **Task 6:** Implement SP decoder and deterministic packer

### Wave 3
- **Task 3:** Patch heuristic bugs (AR ~0.5, boundary packing, compactor)
- **Task 7:** Build training dataset pipeline

### Wave 4
- **Task 4:** Phase 1 full validation gate (target: score ≤ 1.6)
- **Task 8:** Implement Graph Transformer model

### Wave 5
- **Task 9:** Train and select model checkpoint (18-36 GPU-hours/run)

### Wave 6
- **Task 10:** Integrate ML inference with heuristic legalizer

### Wave 7
- **Task 11:** Final QA and score gate (target: score < 1.0)

### Wave 8
- **Task 12:** Package and atomic commit release

## Success Criteria

**Primary:**
- Corrected local validation score < 1.0
- 100/100 feasible
- Runtime < 100ms typical, < 500ms hard ceiling
- No hard-constraint violations

**Secondary:**
- Score 1.0-1.1 with fast runtime (official speed discount likely)
- If ML misses, Phase 1 heuristic score ≤ 1.6 as safe fallback

## Commit Strategy

1. `Fix official score weighting in diagnostics`
2. `Add heuristic regression tests for high-count packing`
3. `Patch heuristic aspect ratio and boundary compaction`
4. `Add sequence-pair label and packing utilities`
5. `Add FloorSet ML dataset pipeline`
6. `Add graph transformer training pipeline`
7. `Integrate ML inference with heuristic legalizer`
8. `Record final validation and release metadata`

---

*Generated by Sisyphus adversarial planning. Team members: pragmatist, deep-diver, architect, creative, strategist.*
